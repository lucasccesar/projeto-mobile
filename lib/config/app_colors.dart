import 'package:flutter/material.dart';

class AppColors {

  // Clube do Livro
  static const Color clube = Color(0xFF4A7FA5);

  // Favoritos
  static const Color favoritos = Color(0xFFC0624E);

  // Catálogo / Busca
  static const Color catalogo = Color(0xFF7A8C63);

  // Compra / Edição
  static const Color compra = Color(0xFF7B5EA7);

  // Coleção
  static const Color colecao = Color(0xFFB8863A);
  
  // Fundo geral
  //static const Color background = Color(0xFFF7F3EE);

  // Texto principal
  //static const Color textPrimary = Color(0xFF5A4633);
}